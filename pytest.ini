[pytest]
testpaths = tests
pythonpath = Workflows/04_scripts
markers =
    integration: requires pixi envs and external data — run with -m integration
    snakemake: requires snakemake CLI on PATH — run with -m snakemake
addopts = -v --tb=short
