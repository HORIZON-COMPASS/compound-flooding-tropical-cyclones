# Defining parameters to run the snakemake pipeline

root_dir:    11210471-001-compass     # root directory of your project, it is appended to the disk drive name specified in the smk files (e.g. c:/ or  p:/)
dir_data:    01_Data              # Folder where data to build the models is saved              
dir_models:  02_Models            # Folder where the base models will be saved (without dynamic forcing data)
dir_runs:    03_Runs              # Folder where model runs will be saved (with forcing)

runname_ids: 
    # EtaIota:                                               # name of the model run     
    #     tc_name: EtaIota                                   # name of the storm. For tropical cyclones, this can be the name of the TC 
    #     region: honduras                                   # name of the region will be used in naming folders                
        
    #     #Meteo forcing
    #     start_time: 20201101 000000                        # Start time of the model run in format: YYYYMMDD HHMMSS                     
    #     end_time: 20201130 000000                          # End time of the model run in format: YYYYMMDD HHMMSS              
    #     precip_forcing: era5_hourly_zarr                   # DataCatalog handle for forcing dataset for the rainfall data  
    #     wflow_meteo_forcing: era5_hourly_zarr               # forcing dataset containing temperature, in- and out-going radiation   
    #     wind_forcing: no_wind                              # wind and pressure dataset (used for coastal surge modelling)     
        
    #     # Cross-model settings
    #     bathy: gebco2024_HND                               # DataCatalog handle for the bathymetry dataset   
    #     river_upa: 30                                      # minimum upstream area threshold for the SFINCS and wflow [km2]
        
    #     #Models selections
    #     use_dfm: False                                     # use the coastal hydrodynamic model D-FM      
    #     use_wflow: True                                    # use the hydrological model Wflow

    #     #DFM model user-defined settings
    #     bbox_dfm: "[-89.0,-82.4,14.9,17.7]"                # Bounding box for the coastal hydrodynamic model in format: [lon_min, lon_max, lat_min, lat_max]    
    #     dfm_res: '450'                                     # coastal hydrodynamic model maximum (finest) resolution in meters (resolution at the coast)                     
    #     dfm_dxy_base: '0.2'                                # coastal hydrodynamic model base grid resolution in degrees (this is the width of the largest grid cells)                                    
    #     tidemodel: 'GTSMv41opendap'                        # tidal model for boundary conditions as can be read by dfm_tools: FES2014, FES2012, EOT20, GTSMv4.1, GTSMv4.1_opendap                     
    #     dfm_obs_file: coastal_coupling_DFM_obs_points_HND  # DataCatalog handle for the coastal points used to couple coastal hydro model D-FM and the flood model SFINCS         
    #     dfm_verification_points:  test                     # DataCatalog handle for the additional points where D-FM output will be saved for validation purposes - check this  
        
    #     #Wflow model user-defined settings
    #     config_wflow_base: wflow_base_build_HND.yml            # name of the wflow configuration file under 05_config_models/01_wflow (default: wflow_base_build.yml)    
    #     use_bankfull_corr: False    
    #     lulc_mapping_wflow: gdlc10m_mapping_wflow  
        
    #     #SFINCS model user-defined settings
    #     config_sfincs_base: sfincs_base_build_honduras.yml # name of the SFINCS configuration file (default: sfincs_base_build.yml)             
    #     sfincs_obs_file: obs_locs_EtaIota.geojson
    #     bbox_sfincs: "[-88.065,15.175,-87.63,16.0]"             # Bounding box for the flood model in format: [lon_min, lat_min, lon_max, lat_max]  
    #     utmzone: 16p                                       # UTM zone (needed by SFINCS, see https://www.latlong.net/lat-long-utm.html)     
    #     coastal_ts: gtsm_codec_reanalysis_hourly_v3        # DataCatalog handle for the coastal boundary conditions if D-FM is NOT used   # gtsm_codec_reanalysis until 2018 only
    #     dfm_coastal_mask: coastal_coupling_msk_HND         # DataCatalog handle for the coastal coupling mask  
    #     lulc_mapping_sfincs: gdlc10m_mapping_sfincs

    #     # Counterfactual forcing settings
    #     CF_value_rain: [0] #[0, -8] # % change
    #     CF_value_wind: [0] # % change
    #     CF_value_SLR: [0]  # absolute change in sea level rise in meters   
    #     CF_landuse: [gdlc_10m_v1_2020_HND] # [gdlc_10m_v1_2020_HND_CF] data catalog handles of land use scenarios    
    Iota:                                               # name of the model run     
        tc_name: Iota                                   # name of the storm. For tropical cyclones, this can be the name of the TC 
        region: honduras                                   # name of the region will be used in naming folders                
        
        #Meteo forcing
        start_time: 20201112 120000                        # Start time of the model run in format: YYYYMMDD HHMMSS                     
        end_time: 20201130 000000                          # End time of the model run in format: YYYYMMDD HHMMSS              
        precip_forcing: era5_hourly_zarr                   # DataCatalog handle for forcing dataset for the rainfall data  
        wflow_meteo_forcing: era5_hourly_zarr               # forcing dataset containing temperature, in- and out-going radiation   
        wind_forcing: no_wind                              # wind and pressure dataset (used for coastal surge modelling)     
        
        # Cross-model settings
        bathy: gebco2024_HND                               # DataCatalog handle for the bathymetry dataset   
        river_upa: 30                                      # minimum upstream area threshold for the SFINCS and wflow [km2]
        
        #Models selections
        use_dfm: False                                     # use the coastal hydrodynamic model D-FM      
        use_wflow: True                                    # use the hydrological model Wflow

        #DFM model user-defined settings
        bbox_dfm: "[-89.0,-82.4,14.9,17.7]"                # Bounding box for the coastal hydrodynamic model in format: [lon_min, lon_max, lat_min, lat_max]    
        dfm_res: '450'                                     # coastal hydrodynamic model maximum (finest) resolution in meters (resolution at the coast)                     
        dfm_dxy_base: '0.2'                                # coastal hydrodynamic model base grid resolution in degrees (this is the width of the largest grid cells)                                    
        tidemodel: 'GTSMv41opendap'                        # tidal model for boundary conditions as can be read by dfm_tools: FES2014, FES2012, EOT20, GTSMv4.1, GTSMv4.1_opendap                     
        dfm_obs_file: coastal_coupling_DFM_obs_points_HND  # DataCatalog handle for the coastal points used to couple coastal hydro model D-FM and the flood model SFINCS         
        dfm_verification_points:  test                     # DataCatalog handle for the additional points where D-FM output will be saved for validation purposes - check this  
        
        #Wflow model user-defined settings
        config_wflow_base: wflow_base_build_HND.yml            # name of the wflow configuration file under 05_config_models/01_wflow (default: wflow_base_build.yml)    
        use_bankfull_corr: False    
        lulc_mapping_wflow: gdlc10m_mapping_wflow  
        
        #SFINCS model user-defined settings
        config_sfincs_base: sfincs_base_build_honduras.yml # name of the SFINCS configuration file (default: sfincs_base_build.yml)             
        sfincs_obs_file: obs_locs_EtaIota.geojson
        bbox_sfincs: "[-88.065,15.175,-87.63,16.0]"             # Bounding box for the flood model in format: [lon_min, lat_min, lon_max, lat_max]  
        utmzone: 16p                                       # UTM zone (needed by SFINCS, see https://www.latlong.net/lat-long-utm.html)     
        coastal_ts: gtsm_codec_reanalysis_hourly_v3        # DataCatalog handle for the coastal boundary conditions if D-FM is NOT used   # gtsm_codec_reanalysis until 2018 only
        dfm_coastal_mask: coastal_coupling_msk_HND         # DataCatalog handle for the coastal coupling mask  
        lulc_mapping_sfincs: gdlc10m_mapping_sfincs

        # Counterfactual forcing settings
        CF_value_rain: [0] #[0, -8] # % change
        CF_value_wind: [0] # % change
        CF_value_SLR: [0]  # absolute change in sea level rise in meters   
        CF_landuse: [gdlc_10m_v1_2020_HND] # [gdlc_10m_v1_2020_HND_CF] data catalog handles of land use scenarios                   